add_namespace = sov_debuff

country_event = { # 5% consumer goods debuff
    id = sov_debuff.1
	hidden = yes
    
	trigger = {
		original_tag = SOV
		date > 1940.01.01
	}
	
	fire_only_once = yes
    
    option = {
        name = sov_debuff.1.a
		add_to_variable = { SOV_fyp_consumer_goods_factor = 0.05 }
    }
	
}