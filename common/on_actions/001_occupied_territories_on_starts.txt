on_actions = {
	on_startup = {
		effect = {

every_state = { limit = { NOT = { is_controlled_by = GRE }} remove_claim_by = GRE }
every_state = { limit = { NOT = { is_controlled_by = AUS }} remove_claim_by = AUS }
every_state = { limit = { NOT = { is_controlled_by = SWI }} remove_claim_by = SWI }
every_state = { limit = { NOT = { is_controlled_by = ARG }} remove_core_of = ARG remove_claim_by = ARG }
every_state = { limit = { NOT = { is_controlled_by = IRE }} remove_core_of = IRE remove_claim_by = IRE }
every_state = { limit = { NOT = { is_controlled_by = LIT }} remove_core_of = LIT remove_claim_by = LIT }
every_state = { limit = { NOT = { is_controlled_by = PRC }} remove_core_of = PRC remove_claim_by = PRC }
every_state = { limit = { NOT = { is_controlled_by = MEN }} remove_core_of = MEN remove_claim_by = MEN }
every_state = { limit = { NOT = { is_controlled_by = YUG }} remove_core_of = YUG remove_claim_by = YUG }

every_state = { limit = { is_controlled_by = USA } add_core_of = USA }

every_state = { limit = { is_core_of = AFA } ETH = { annex_country = { target = AFA transfer_troops = yes }} remove_core_of = AFA }


every_state = { remove_core_of = PAN remove_claim_by = PAN }
every_state = { remove_core_of = TIB remove_claim_by = TIB }


			}
		}
	}
