change_host_tool = {

 	change_to_GER = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = GER
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				GER = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_ITA = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = ITA
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				ITA = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_ROM = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = ROM
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				ROM = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_HUN = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = HUN
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				HUN = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_BUL = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = BUL
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				BUL = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_FIN = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = FIN
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				FIN = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_SWE = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = SWE
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				SWE = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_USA = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = USA
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				USA = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_CAN = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = CAN
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				CAN = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_SAF = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = SAF
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				SAF = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_RAJ = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = RAJ
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				RAJ = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_AST = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = AST
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				AST = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_NZL = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = NZL
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				NZL = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_JAP = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = JAP
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				JAP = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_ARG = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = ARG
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				ARG = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_SOV = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = SOV
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				SOV = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_FRA = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = FRA
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				FRA = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
 
 	change_to_BRA = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = BRA
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				BRA = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	} 

 	change_to_MEX = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = MEX
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				MEX = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_COL = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = COL
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				COL = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_SPR = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = SPR
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				SPR = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_ENG = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = ENG
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				ENG = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_POR = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = POR
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				POR = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 
 	change_to_PER = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = PER
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				PER = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
	
 	change_to_POL = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = POL
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				POL = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}
 
 	change_to_TUR = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = TUR
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				TUR = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

 	change_to_GRE = {
 		allowed = {
 		}
 		visible = {
			NOT = {
				tag = GRE
			}
 			has_country_flag = host
 		}
 		fire_only_once = no
 		complete_effect = {
 				clr_country_flag = host
				
				GRE = {
				
					set_country_flag = host
				
				}
				
 			}
 		ai_will_do = {
 			base = 0
 		}
 	}

}

