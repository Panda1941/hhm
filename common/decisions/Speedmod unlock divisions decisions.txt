#############################
# remove effects =
# complete_effects =
# activation =
# days_mission_timeout = 360
# days_remove = 15
# days_re_enable = 60
#############################

operations = {

    unlock_divisions_POL = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {POL = { is_ai = yes } GER = {has_completed_focus = GER_mol_rib_pact}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU POL = { is_ai = yes }}
	targets = {POL}
	complete_effect = {		
		POL = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			load_oob = 20_w_POL_danzig_or_war
			add_manpower = -240000
			}
		}
    }

    unlock_divisions_DEN = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {DEN = { is_ai = yes } GER = {has_completed_focus = GER_danzig_or_war}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU DEN = { is_ai = yes }}
	targets = {DEN}
	complete_effect = {
		DEN = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_NOR = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {NOR = { is_ai = yes } GER = {has_completed_focus = GER_danzig_or_war}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU NOR = { is_ai = yes }}
	targets = {NOR}
	complete_effect = {
		NOR = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_HOL = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {HOL = { is_ai = yes } GER = {has_completed_focus = GER_mol_rib_pact}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU HOL = { is_ai = yes }}
	targets = {HOL}
	complete_effect = {
		HOL = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_YUG = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {YUG = { is_ai = yes } GER = {has_completed_focus = GER_anschluss}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU YUG = { is_ai = yes }}
	targets = {YUG}
	complete_effect = {
		YUG = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_GRE = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {GRE = { is_ai = yes } GER = {has_completed_focus = GER_anschluss}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU GRE = { is_ai = yes }}
	targets = {GRE}
	complete_effect = {
		GRE = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }


    unlock_divisions_FIN_winter_war = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {FIN = { is_ai = yes } SOV = {has_completed_focus = SOV_claims_in_baltic}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU FIN = { is_ai = yes }}
	targets = {FIN}
	complete_effect = {
		FIN = {
			country_lock_all_division_template = no
			add_ideas = {
				extensive_conscription
				ai_faster_training
				}
				load_oob = 10_w_FIN_winter_war
				add_manpower = -80000
			}
        }
    }

    unlock_divisions_INS = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {INS = { is_ai = yes } HOL = { has_capitulated = yes }}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU INS = { is_ai = yes }}
	targets = {INS}
	complete_effect = {
		INS = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_PER = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {PER = { is_ai = yes } SOV = {has_completed_focus = SOV_preemptive_invasion_of_iran}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU PER = { is_ai = yes }}
	targets = {PER}
	complete_effect = {
		PER = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_IRQ = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {IRQ = { is_ai = yes } FRA = { has_capitulated = yes }}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU IRQ = { is_ai = yes }}
	targets = {IRQ}
	complete_effect = {
		IRQ = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_SIA = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {SIA = { is_ai = yes } JAP = {has_completed_focus = JAP_strike_south_doctrine}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU SIA = { is_ai = yes }}
	targets = {SIA}
	complete_effect = {
		SIA = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }

    unlock_divisions_CHI = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
#	available = {CHI = { is_ai = yes } JAP = {has_completed_focus = JAP_liaison_conference}}
	available = {CHI = { is_ai = yes } JAP = {has_completed_focus = JAP_greater_east_asian_co_prosperity_sphere}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU CHI = { is_ai = yes }}
	targets = {CHI}
	complete_effect = {
		CHI = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			load_oob = 20_w_CHI_marco_polo_bridge
			add_manpower = -247200
			}
        }
    }

    unlock_divisions_PHI = {
	#icon = generic_operation
	icon = generic_formable_nations
	allowed = {tag = NEU}
	available = {PHI = { is_ai = yes } JAP = {has_completed_focus = JAP_strike_south_doctrine}}
	fire_only_once = yes
        ai_will_do = {factor = 1}
	visible = {tag = NEU PHI = { is_ai = yes }}
	targets = {PHI}
	complete_effect = {
		PHI = {
			country_lock_all_division_template = no
			add_ideas = { ai_faster_training }
			}
        }
    }


}

